
const Quiz = (props) => {
    // console.log(props.quizQuestions)
    // const { questions } = props.quizQuestions;
    return (
      <>
        <h2>quiz here</h2>
        <p>Question: {props.quizQuestions}</p>
      </>
    );
}

export default Quiz;

